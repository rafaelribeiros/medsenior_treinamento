/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {TreinamentoApp} from './src';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TreinamentoApp);
